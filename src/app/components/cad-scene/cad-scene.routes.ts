import { Route } from '@angular/router';
import { CadSceneComponent } from './cad-scene.component';

export const CadSceneRoutes: Route[] = [
  {
    path: '',
    component: CadSceneComponent,
  },
];
